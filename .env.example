# ===== 复制本文件为 .env，填入你的密钥（.env 会被 .gitignore 忽略，不要上传）=====

# 大模型（DeepSeek，OpenAI 兼容接口，官网 https://platform.deepseek.com/ 申请）
DEEPSEEK_API_KEY=sk-你的DeepSeek密钥

# 可选：开启高危工具 run_python（执行任意 Python 代码）
# 默认关闭。即使开启，它也会在"子进程沙箱"里运行（无 API key、超时强杀、输出截断）。
# ENABLE_RUN_PYTHON=1
