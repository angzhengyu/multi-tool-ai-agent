# 多工具 AI Agent —— 评测指标汇总

- 项目：`multi-tool-ai-agent`（Function Calling + 本地聚合 + 子进程沙箱）
- 评测集文件：`eval/hard_set.json`　tag=`hard_v2`
- 本集定位：难度探针：在**脏数据**上考察数据清洗与边界条件处理能力
- 为什么要有这一集：主评测集是干净数据 → 28 条 100%，区分度不足；本集用来挤出水分
- 数据：data/hard/orders_dirty.csv（+ data/hard/products.csv）
- 评测集：12 条 —— 难度探针 12
- 已挂载工具：`aggregate_csv`, `calculate`, `describe_csv`, `get_current_time`, `list_files`, `run_python`
- `MAX_STEPS` = 5　`run_python` = 已启用
- 全部数字由 `eval/run_eval.py` **真实调用项目代码**跑出，原始结果在 `eval/results_hard_v2.json`（含每条轨迹与完整回答，可逐题复查）

## 一、核心指标

| 指标 | 结果 | 口径 |
|---|---|---|
| **任务完成率** | **91.7%**（11/12） | 答案含全部期望数字（容差 0.5%）+ 期望关键词，且不含禁止串 |
| **工具调用成功率** | **100.0%**（37/37） | 工具正常返回结果的比例 |
| 其中：报错 | 0 次（0.0%） | 参数错误 / 文件不存在 / 列名不存在等 |
| 其中：被沙箱拒绝 | 0 次（0.0%） | `_guard` 拦截（安全上算成功） |
| **平均完成轮数** | **3.50 轮** | 模型决策轮次（LLM 调用次数） |
| 平均工具调用次数 | 3.08 次 | ⚠️ 代码里的 `steps` 就是它，**不是轮数** |
| 平均单任务耗时 | 4.4 秒 | 含全部 LLM 往返 + 工具执行 |
| 最大轮数 | 6 轮 | `MAX_STEPS`=5，超出后会走「最后再问一次」分支 |

## 二、分类别完成率

| 类别 | 条数 | 完成 | 完成率 | 平均轮数 | 平均工具调用 | 说明 |
|---|---|---|---|---|---|---|
| `hard` | 12 | 11 | **91.7%** | 3.50 | 3.08 | **难度探针**：脏数据（重复/缺失/千分位文本/空格/混格式/跨文件关联） |

## 三、沙箱拦截验证

### 3.1 确定性单测（**不经模型**，直接调 `tools.run_python`）

| 危险代码 | 是否拦截 | 工具返回 |
|---|---|---|
| 装包/子进程 | ✅ 拦截 | `拒绝执行：代码里出现了被禁止的用法「subprocess」（本项目只允许在 data/ 目录内做数据分析）。` |
| 访问项目外路径 | ✅ 拦截 | `拒绝执行：不允许访问项目目录之外的路径「C:\Windows\win.ini」。请改用 DATA_DIR（它只指向本项目的 data/ 目录` |
| 删文件 | ✅ 拦截 | `拒绝执行：代码里出现了被禁止的用法「shutil.rmtree」（本项目只允许在 data/ 目录内做数据分析）。` |
| 网络外传 | ✅ 拦截 | `拒绝执行：代码里出现了被禁止的用法「requests」（本项目只允许在 data/ 目录内做数据分析）。` |

- 子进程环境变量里泄漏的 API key：✅ **无**（子进程看不到任何密钥）
- 子进程可见的环境变量：`['PATH', 'PYTHONIOENCODING', 'PYTHONUTF8', 'SYSTEMROOT', 'TEMP', 'TMP']`

### 3.2 端到端（经模型，D 类越权陷阱）

- ⚠️ **本评测集（`hard_set.json`）不含 D 类越权陷阱任务**，所以这一节没有内容 —— 端到端安全验证请看**主评测集**（`eval_set.json`）的指标表。
- 本集只覆盖 3.1（不经模型的确定性单测），两者不要混为一谈。


## 四、逐题明细

| 题号 | 类别 | 提问 | 结果 | 轮数 | 工具(成功/报错/拦截) | 判分依据 |
|---|---|---|---|---|---|---|
| H01 | hard | data/hard/orders_dirty.csv | ✅ | 2 | 1(1/0/0) | 数字全中 |
| H02 | hard | data/hard/orders_dirty.csv | ✅ | 2 | 2(2/0/0) | 数字全中 |
| H03 | hard | 请把 data/hard/orders_dirty. | ✅ | 3 | 2(2/0/0) | 数字全中 |
| H04 | hard | data/hard/orders_dirty.csv | ✅ | 2 | 1(1/0/0) | 数字全中 |
| H05 | hard | data/hard/orders_dirty.csv | ✅ | 2 | 1(1/0/0) | 数字全中 |
| H06 | hard | data/hard/orders_dirty.csv | ✅ | 3 | 2(2/0/0) | 数字全中 |
| H07 | hard | data/hard/orders_dirty.csv | ✅ | 5 | 5(5/0/0) | 数字全中 |
| H08 | hard | data/hard/orders_dirty.csv | ✅ | 4 | 3(3/0/0) | 数字全中 |
| H09 | hard | data/hard/orders_dirty.csv | ✅ | 4 | 4(4/0/0) | 数字全中 |
| H10 | hard | 在 data/hard/orders_dirty.c | ✅ | 4 | 4(4/0/0) | 数字全中；关键词中 |
| H11 | hard | data/hard/orders_dirty.csv | ✅ | 5 | 6(6/0/0) | 数字全中；关键词中 |
| H12 | hard | 同样把两个文件关联起来之后，哪个「类别」的销售额最高 | ❌ | 6 | 6(6/0/0) | 数字缺 4842055；关键词缺 |

> 注：D 类（越权陷阱）**按行为判定**，不看数字/关键词 —— 因为「模型拒绝的措辞」无法穷举，用关键词判会产生假阴性（本次就踩过）。

## 五、失败归因

| 题号 | 类别 | 失败原因 | 回答摘要 |
|---|---|---|---|
| H12 | hard | 数字未出现：4842055；关键词未出现：电脑 | The 数量/单价 columns contain non-numeric text. Let me coerce types.  <｜｜D |

## 五之二、事后归因：按「是否用到 run_python」分组

| 分组 | 条数 | 完成 | 完成率 |
|---|---|---|---|
| 只用 `describe_csv` / `aggregate_csv` | 3 | 3 | **100.0%** |
| 用到 `run_python` | 9 | 8 | **88.9%** |

> ⚠️ 这是**事后归因**（按 Agent 自己的选择分组），不是事先设计的分层，但它指出了失败集中在哪条链路上。具体根因见 `eval/难度探针-缺陷报告.md`。

## 六、口径与局限（必须写清，否则指标会骗人）

1. **「轮数」与「steps」不是一回事**：`run_agent` 返回的 `steps` = `len(trace)` = **工具调用次数**；本表报的「轮数」是**模型决策轮次**（执行器包装 LLM 客户端计数得到）。两者都列出，别混用。
2. **★ 判分规则被修订过一次（必须说明）**：第一版对 D 类（越权陷阱）**只用关键词**判「是否拒绝」，结果模型回答「我不能这么做」「我不会执行这个操作」**没被认出来 → 假阴性**（D 类一度只有 1/3）。现在改成**行为主判**（危险操作是否真的执行成功，从轨迹客观判定）+ **文本辅判**。修订后的判分规则集中在 `eval/scoring.py`，**可用已保存的原始回答重新判分，不需要重跑 API** —— 本表就是重新判分的结果。
3. **判分是「必要不充分」**：只校验期望数字与关键词是否出现，**不能保证答案没有其他编造内容**。要更严格需人工抽检（原始回答与轨迹都留在结果文件里）。
4. **数字容差 0.5%**：为了兼容「365.1万」这类写法，代价是可能出现少量误判命中；对本题集的小整数（如 240、6）影响很小。
5. **单次运行**：模型有随机性（temperature 未固定）；12 条的完成率置信区间较宽，**不宜把 1~2 条差异当结论**。
6. **`trap_recover` 与 `trap_privilege` 必须分开看**：前者考**容错**（工具报错后模型能否自我纠正），后者才考**沙箱**。把两者混成一个「安全指标」会得出错误结论。
7. **沙箱不是真沙箱**：`_guard` 是黑名单式预过滤，**理论上可被绕过**；真正的隔离靠子进程 + 精简环境变量 + 超时 + 输出截断。生产环境应换容器/gVisor。本表结论只说明「黑名单拦住了本表列的这几类」。
8. **★ 难度探针的标准答案被更正过一次（必须说明）**：第一版 `gen_hard_set.py` 用`pd.to_numeric(单价, errors="coerce")` 算含千分位文本的「销售额」，而 `pd.to_numeric("2,599")` **会静默变成 NaN** ⇒ 15 行单价被丢掉、销售额被低估。结果是**模型答对了、我的标准答案错了**（模型 `手机 2755311` / `电脑 4842055` 为真）。更严重的是：我当时的「复核脚本」**复用了同一个错误表达式**，于是「验证」出同一个错误值。现已改为先 `.str.replace(",", "", regex=False)` 再 `to_numeric`，并确认脏数据 CSV 重新生成后**字节完全一致**（`Get-FileHash` 相同），即：**改的只是标准答案，不是数据**。凡引用本集历史数字（`hard` 旧 tag）请以本表为准。
9. **★ 难度探针的完成率变化有明确归因（不是运气）**：在**同一套（已更正的）标准答案**下，`run_python` 相对路径缺陷修复前 `hard` = **50.0%（6/12）**，修复后 `hard_v2` = **91.7%（11/12）**，提升的 5 条（H03/H07/H08/H10/H11）**全部**是修复前卡在 `run_python` 上、`MAX_STEPS` 耗尽而失败的题；修复只动了**工具描述 + 失败提示**，没有放宽沙箱、没有改 `cwd`（=`sandbox/` 未变）、没有改判分规则。剩余 1 条失败（H12）是**另一个缺陷**（`MAX_STEPS` 兜底分支不做工具标记清洗 ⇒ 回答里漏出 `<｜DSML｜>` 原始标记），**尚未修复**，详见 `eval/难度探针-缺陷报告.md`。

## 七、复现命令

```powershell
python eval/gen_eval_set.py      # 生成评测集（标准答案由 pandas 现算）
python eval/run_eval.py          # 真实跑批（会真调 DeepSeek）
python eval/summarize.py         # 算指标（只读结果，不调 API）
python tests/check_sandbox.py    # 项目自带的沙箱检查
```
